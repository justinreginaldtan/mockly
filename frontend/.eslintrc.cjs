module.exports = {
  root: true,
  extends: ["next"],
  parserOptions: {
    tsconfigRootDir: __dirname,
  },
  rules: {
    // Keep rules lightweight for hackathon velocity; tighten as needed later.
  },
}
